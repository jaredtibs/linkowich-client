import React, { Component, PropTypes } from 'react';

class Publish extends Component {
  constructor(props) {
    super(props)
  }

  render() {
    return(
      <div>
      </div>
    )
  }
}

export default Publish
